所有GPIO都挂靠在APB2这个外设总线
每个gpio外设按照GPIOA,GPIOB等等这样命名
每个GPIO外设总共有16个引脚，编号从0(PA0)-15(PA15)

主要包含寄存器和驱动器

寄存器:特殊存储器,内核可以通过APB2总线对寄存器进行读写 从而完成输出电平和读取电平的功能
每一位对应一个引脚,输出寄存器写1,对应引脚就输出高电平;写0,就输出低电平
输入寄存器读取为1,则为高电平;反之则为低电平  
STM32内部的寄存都是32位 但端口只有16位 所以只有低16位对应的有端口

驱动器:增加信号的驱动能力


