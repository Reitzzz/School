# sy4 从 0 开始写代码

## 目录

- [1. 这次实验要做什么](#1-这次实验要做什么)
- [2. 先认识硬件和时间关系](#2-先认识硬件和时间关系)
- [3. 本实验需要用到的库函数（考试会给）](#3-本实验需要用到的库函数考试会给)
- [4. 写代码前先分层](#4-写代码前先分层)
- [5. 先写伪代码](#5-先写伪代码)
  - [5.1 怎么得出伪代码](#51-怎么得出伪代码)
  - [5.2 完整伪代码](#52-完整伪代码)
- [6. LED、TIM3、NVIC 驱动怎么写](#6-ledtim3nvic-驱动怎么写)
- [7. 中断服务函数怎么写](#7-中断服务函数怎么写)
- [8. 主函数怎么写](#8-主函数怎么写)
- [9. 实验报告要截图的代码](#9-实验报告要截图的代码)
- [10. 最容易错的地方](#10-最容易错的地方)


## 1. 这次实验要做什么

`1904300101张三@实验报告4.doc` 要求用 **TIM3 基本定时功能** 实现周期为 1 秒的定时中断：

- 创建工程并加入 GPIO、RCC、TIM、NVIC 标准库。
- TIM3 每隔 1 秒产生一次更新中断。
- 在 `TIM3_IRQHandler()` 中让 LED0、LED1 同时闪烁一次。

一句话：sy4 是“定时器中断点灯”。

## 2. 先认识硬件和时间关系

| 对象 | 连接/参数 | 作用 |
| --- | --- | --- |
| LED0 | PB5 | 输出，低电平点亮 |
| LED1 | PE5 | 输出，低电平点亮 |
| TIM3 | APB1 总线 | 每 1 秒产生一次更新中断 |
| TIM3 中断 | `TIM3_IRQn` | 进入 `TIM3_IRQHandler()` |

定时器最重要的公式：

```text
定时时间 = (PSC + 1) * (ARR + 1) / TIMx_CLK
```

本实验按 TIM3 时钟 72MHz 计算：

```text
PSC = 7199
ARR = 9999
定时时间 = 7200 * 10000 / 72000000 = 1 秒
```

## 3. 本实验需要用到的库函数（考试会给）

| 名称 | 作用 | 函数参数 |
| --- | --- | --- |
| `RCC_APB2PeriphClockCmd()` | 打开 GPIO 时钟，可近似理解为给 GPIO “通电”，让它开始工作 | 外设时钟名，开关状态<br>`RCC_APB2Periph_GPIOB`、`ENABLE` |
| `RCC_APB1PeriphClockCmd()` | 打开 TIM3 时钟，可近似理解为给 TIM3 “通电”，让它开始工作 | 外设时钟名，开关状态<br>`RCC_APB1Periph_TIM3`、`ENABLE` |
| `GPIO_InitTypeDef` | GPIO 初始化结构体类型，用它定义 GPIO 配置表变量。声明本身只定义变量，还没有配置硬件 | 变量名<br>`GPIO_InitStructure` |
| `GPIO_InitStructure.GPIO_Pin` | 配置要初始化的 GPIO 引脚，可以单个引脚，也可以用 `\|` 同时选择多个引脚 | GPIO 引脚<br>`GPIO_Pin_5` |
| `GPIO_InitStructure.GPIO_Mode` | 配置 GPIO 工作模式，决定这个引脚是输入、输出，还是复用功能 | GPIO 工作模式<br>`GPIO_Mode_Out_PP`（推挽输出） |
| `GPIO_InitStructure.GPIO_Speed` | 配置 GPIO 输出速度；只有输出或复用输出时需要重点设置 | GPIO 输出速度<br>`GPIO_Speed_50MHz` |
| `TIM_TimeBaseInitTypeDef` | TIM 基本计数结构体类型，用它定义定时器配置表变量。声明本身只定义变量，还没有配置硬件 | 变量名<br>`TIM_TimeBaseStructure` |
| `NVIC_InitTypeDef` | NVIC 初始化结构体类型，用它定义 NVIC 配置表变量。声明本身只定义变量，还没有配置硬件 | 变量名<br>`NVIC_InitStructure` |
| `GPIO_Init()` | 初始化 GPIO，真正把 GPIO 结构体配置写进硬件 | GPIO 端口，GPIO 初始化结构体地址<br>`GPIOB`、`&GPIO_InitStructure` |
| `TIM_TimeBaseInit()` | 初始化定时器基本计数，真正把 TIM 结构体配置写进硬件 | TIM 外设，TIM 基本计数结构体地址<br>`TIM3`、`&TIM_TimeBaseStructure` |
| `TIM_ITConfig()` | 使能 TIM 更新中断 | TIM 外设，中断类型，开关状态<br>`TIM3`、`TIM_IT_Update`、`ENABLE` |
| `NVIC_Init()` | 配置 NVIC | NVIC 初始化结构体地址<br>`&NVIC_InitStructure` |
| `TIM_Cmd()` | 所有配置完成后启动定时器 | TIM 外设，开关状态<br>`TIM3`、`ENABLE` |
| `TIM_GetITStatus()` | 中断发生后，判断中断标志 | TIM 外设，中断类型<br>`TIM3`、`TIM_IT_Update` |
| `TIM_ClearITPendingBit()` | 中断处理结束前，清除中断标志 | TIM 外设，中断类型<br>`TIM3`、`TIM_IT_Update` |

## 4. 写代码前先分层

- `USER_DEVICE_DRIVER/led_driver.c/.h`：配置 LED GPIO、TIM3、NVIC，提供 LED 闪烁函数。
- `USER_APP/it.c`：写 `TIM3_IRQHandler()`。
- `USER_APP/main.c`：调用初始化函数，然后空循环。

驱动层负责把“1 秒闹钟”设置好；`it.c` 负责闹钟响了以后做什么。

## 5. 先写伪代码

### 5.1 怎么得出伪代码

第一步，实验要求“每隔 1 秒”，所以要配置 TIM3 的 PSC 和 ARR。

第二步，实验要求“通过更新中断方式点灯”，所以要打开 `TIM_IT_Update` 和 `TIM3_IRQn`。

第三步，实验要求“LED0、LED1 闪烁一次”，所以中断服务函数中调用 `LED01_Flash()`。

### 5.2 完整伪代码

```text
开始

初始化 LED 和 TIM3
    打开 GPIOB、GPIOE 时钟
    PB5、PE5 配置为推挽输出
    默认熄灭 LED0、LED1
    打开 TIM3 时钟
    设置 PSC = 7199
    设置 ARR = 9999
    使能 TIM3 更新中断
    在 NVIC 中打开 TIM3_IRQn
    启动 TIM3

主循环
    什么都不用做，等待 TIM3 中断

如果进入 TIM3_IRQHandler
    检查是不是 TIM3 更新中断
    LED0、LED1 同时闪烁一次
    清除 TIM3 更新中断标志
```

## 6. LED、TIM3、NVIC 驱动怎么写

```c
#include "stm32f10x_conf.h"
#include "sw_delay.h"
#include "misc.h"

void LED_GPIO_TIM3_NVIC_Config(void)
{
    GPIO_InitTypeDef GPIO_InitStructure;
    TIM_TimeBaseInitTypeDef TIM_TimeBaseStructure;
    NVIC_InitTypeDef NVIC_InitStructure;

    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB | RCC_APB2Periph_GPIOE, ENABLE);

    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_5;
    GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
    GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
    GPIO_Init(GPIOE, &GPIO_InitStructure);

    GPIO_SetBits(GPIOB, GPIO_Pin_5);
    GPIO_SetBits(GPIOE, GPIO_Pin_5);

    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM3, ENABLE);

    TIM_TimeBaseStructure.TIM_Period = 10000 - 1;
    TIM_TimeBaseStructure.TIM_Prescaler = 7200 - 1;
    TIM_TimeBaseStructure.TIM_ClockDivision = TIM_CKD_DIV1;
    TIM_TimeBaseStructure.TIM_CounterMode = TIM_CounterMode_Up;
    TIM_TimeBaseInit(TIM3, &TIM_TimeBaseStructure);

    TIM_ITConfig(TIM3, TIM_IT_Update, ENABLE);

    NVIC_InitStructure.NVIC_IRQChannel = TIM3_IRQn;
    NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 0;
    NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;
    NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;
    NVIC_Init(&NVIC_InitStructure);

    TIM_Cmd(TIM3, ENABLE);
}

void LED01_Flash(void)
{
    GPIO_ResetBits(GPIOB, GPIO_Pin_5);
    GPIO_ResetBits(GPIOE, GPIO_Pin_5);
    SoftwareDelay(2000000);
    GPIO_SetBits(GPIOB, GPIO_Pin_5);
    GPIO_SetBits(GPIOE, GPIO_Pin_5);
}
```

## 7. 中断服务函数怎么写

```c
#include "led_driver.h"
#include "stm32f10x_conf.h"

void TIM3_IRQHandler(void)
{
    if (TIM_GetITStatus(TIM3, TIM_IT_Update) != RESET)
    {
        LED01_Flash();
        TIM_ClearITPendingBit(TIM3, TIM_IT_Update);
    }
}
```

## 8. 主函数怎么写

```c
#include "led_driver.h"

int main(void)
{
    LED_GPIO_TIM3_NVIC_Config();

    while (1)
    {
    }
}
```

## 9. 实验报告要截图的代码

1. `led_driver.c` 中 `LED_GPIO_TIM3_NVIC_Config()` 和 `LED01_Flash()`。
2. `it.c` 中 `TIM3_IRQHandler()`。
3. `main.c`。

## 10. 最容易错的地方

- `TIM_ITConfig()` 少写第一个参数 `TIM3`，正确写法是 `TIM_ITConfig(TIM3, TIM_IT_Update, ENABLE);`。
- TIM3 在 APB1 上，要用 `RCC_APB1PeriphClockCmd()`。
- PSC 和 ARR 要代入 `(PSC + 1) * (ARR + 1) / 72MHz`。
- 忘记 `TIM_Cmd(TIM3, ENABLE)`：定时器不会开始计数。
- 忘记清除 `TIM_IT_Update` 标志：会反复进入中断。
