# 大数据数学基础 第六章 多元统计分析 学习笔记（强化版）

本章主要讨论多元统计分析中的回归分析、判别分析、聚类分析、主成分分析、因子分析和典型相关分析。  
本笔记重点强化了两类内容：

- **解题算法**：把考试或作业中常用的计算步骤拆成可直接照做的流程。
- **例题详解**：每类方法都给出更醒目的例题，尽量写清楚“先算什么、再比较什么、最后怎么下结论”。

---

## 目录

- [6.1 回归分析](#sec-61)
  - [6.1.1 一元线性回归](#sec-611)
  - [6.1.2 多元线性回归](#sec-612)
  - [6.1.3 Logistic 回归](#sec-613)
- [6.2 判别分析](#sec-62)
  - [6.2.1 距离判别](#sec-621)
  - [6.2.2 贝叶斯判别](#sec-622)
  - [6.2.3 Fisher 判别](#sec-623)
- [6.3 聚类分析](#sec-63)
  - [6.3.1 距离与相似系数](#sec-631)
  - [6.3.2 系统聚类法](#sec-632)
  - [6.3.3 动态聚类法 K-Means](#sec-633)
- [6.4 主成分分析](#sec-64)
- [6.5 因子分析](#sec-65)
  - [6.5.1 因子载荷矩阵的主成分法估计](#sec-651)
  - [6.5.2 主因子法](#sec-652)
  - [6.5.3 因子旋转与因子得分](#sec-653)
- [6.6 典型相关分析](#sec-66)

---

<a id="sec-61"></a>

## 6.1 回归分析

回归分析用于研究因变量与一个或多个自变量之间的数量关系，并利用这种关系进行预测或解释。

<a id="sec-611"></a>

### 6.1.1 一元线性回归

#### 1. 模型与参数估计公式

一元线性回归模型：

$$
\hat{y}=\hat{\beta}_0+\hat{\beta}_1x
$$

其中：

- $\hat{y}$：因变量 $y$ 的预测值。
- $x$：自变量。
- $\hat{\beta}_0$：截距，表示 $x=0$ 时的预测值。
- $\hat{\beta}_1$：斜率，表示 $x$ 每增加 1 个单位，$\hat{y}$ 平均增加的数量。

最小二乘法估计：

$$
\hat{\beta}_1=\frac{L_{xy}}{L_{xx}}
=\frac{\sum_{i=1}^{n}(x_i-\bar{x})(y_i-\bar{y})}{\sum_{i=1}^{n}(x_i-\bar{x})^2}
$$

$$
\hat{\beta}_0=\bar{y}-\hat{\beta}_1\bar{x}
$$

常用平方和：

$$
\text{SST}=\sum(y_i-\bar{y})^2
$$

$$
\text{SSR}=\sum(\hat{y}_i-\bar{y})^2
$$

$$
\text{SSE}=\sum(y_i-\hat{y}_i)^2
$$

三者关系：

$$
\text{SST}=\text{SSR}+\text{SSE}
$$

#### 2. 显著性检验

t 检验用于检验单个回归系数是否显著：

$$
t=\frac{\hat{\beta}_1}{\sqrt{\operatorname{var}(\hat{\beta}_1)}}\sim t(n-2)
$$

F 检验用于检验整个回归方程是否显著：

$$
F=\frac{\text{SSR}/1}{\text{SSE}/(n-2)}\sim F(1,n-2)
$$

判定方法：

- 若 $|t|>t_{\alpha/2}(n-2)$，则认为 $\beta_1$ 显著不为 0。
- 若 $F>F_{\alpha}(1,n-2)$，则认为回归方程整体显著。

#### 3. 置信区间

斜率 $\hat{\beta}_1$ 的置信度为 $1-\alpha$ 的置信区间：

$$
\left[
\hat{\beta}_1-\sqrt{\operatorname{var}(\hat{\beta}_1)}t_{\alpha/2}(n-2),
\hat{\beta}_1+\sqrt{\operatorname{var}(\hat{\beta}_1)}t_{\alpha/2}(n-2)
\right]
$$

#### 【解题算法】一元线性回归标准流程

1. 整理数据表，列出 $x_i,y_i$。
2. 计算样本均值 $\bar{x},\bar{y}$。
3. 计算离差 $x_i-\bar{x}$ 与 $y_i-\bar{y}$。
4. 计算 $L_{xx}=\sum(x_i-\bar{x})^2$。
5. 计算 $L_{xy}=\sum(x_i-\bar{x})(y_i-\bar{y})$。
6. 计算斜率 $\hat{\beta}_1=L_{xy}/L_{xx}$。
7. 计算截距 $\hat{\beta}_0=\bar{y}-\hat{\beta}_1\bar{x}$。
8. 写出回归方程 $\hat{y}=\hat{\beta}_0+\hat{\beta}_1x$。
9. 如需预测，把新的 $x$ 代入回归方程。
10. 如需检验，继续计算 $\text{SSR},\text{SSE},t,F$，再与临界值比较。

#### 【例题 6-1】建立一元线性回归方程

已知某组火灾损失数据中：

$$
\bar{x}=3.28,\quad \bar{y}=26.413,\quad L_{xx}=34.784,\quad L_{xy}=171.114
$$

求经验回归方程。

解：

第一步，计算斜率：

$$
\hat{\beta}_1=\frac{L_{xy}}{L_{xx}}
=\frac{171.114}{34.784}\approx 4.919
$$

第二步，计算截距：

$$
\hat{\beta}_0=\bar{y}-\hat{\beta}_1\bar{x}
=26.413-4.919\times 3.28
$$

$$
\hat{\beta}_0\approx 26.413-16.135=10.278
$$

第三步，写出回归方程：

$$
\hat{y}=10.278+4.919x
$$

结论：

- 当 $x$ 增加 1 个单位时，预测损失 $\hat{y}$ 平均增加约 $4.919$ 个单位。
- 当 $x=0$ 时，模型预测损失约为 $10.278$。

#### 【例题 6-2】利用回归方程进行预测

沿用上题回归方程：

$$
\hat{y}=10.278+4.919x
$$

若 $x=5$，预测 $y$。

解：

$$
\hat{y}=10.278+4.919\times 5
$$

$$
\hat{y}=10.278+24.595=34.873
$$

结论：当 $x=5$ 时，预测值约为 $34.873$。

<a id="sec-612"></a>

### 6.1.2 多元线性回归

#### 1. 模型与参数估计公式

多元线性回归模型：

$$
\boldsymbol{Y}=\boldsymbol{X}\boldsymbol{\beta}+\boldsymbol{\varepsilon}
$$

最小二乘估计：

$$
\hat{\boldsymbol{\beta}}
=
(\boldsymbol{X}^{\text{T}}\boldsymbol{X})^{-1}
\boldsymbol{X}^{\text{T}}\boldsymbol{Y}
$$

其中：

- $\boldsymbol{Y}$：因变量观测值向量。
- $\boldsymbol{X}$：设计矩阵，第一列通常为全 1，用于估计截距。
- $\boldsymbol{\beta}$：待估计的回归系数向量。
- $\boldsymbol{\varepsilon}$：随机误差项。

回归标准差：

$$
\hat{\sigma}=
\sqrt{
\frac{1}{n-p-1}
\sum_{i=1}^{n}(y_i-\hat{y}_i)^2
}
$$

整体显著性的 F 检验：

$$
F=\frac{\text{SSR}/p}{\text{SSE}/(n-p-1)}
$$

单个系数的 t 检验：

$$
t_j=\frac{\hat{\beta}_j}{\hat{\sigma}\sqrt{c_{jj}}}
$$

其中 $c_{jj}$ 是 $(\boldsymbol{X}^{\text{T}}\boldsymbol{X})^{-1}$ 主对角线上的第 $j$ 个元素。

#### 【解题算法】多元线性回归矩阵法

1. 写出设计矩阵 $\boldsymbol{X}$，注意第一列是否需要补 1。
2. 写出因变量向量 $\boldsymbol{Y}$。
3. 计算 $\boldsymbol{X}^{\text{T}}\boldsymbol{X}$。
4. 计算 $\boldsymbol{X}^{\text{T}}\boldsymbol{Y}$。
5. 求逆矩阵 $(\boldsymbol{X}^{\text{T}}\boldsymbol{X})^{-1}$。
6. 相乘得到 $\hat{\boldsymbol{\beta}}=(\boldsymbol{X}^{\text{T}}\boldsymbol{X})^{-1}\boldsymbol{X}^{\text{T}}\boldsymbol{Y}$。
7. 写出回归方程。
8. 若需要检验，计算预测值、残差、SSE、SSR、F 和 t 统计量。

#### 【例题 6-3】由矩阵结果求多元回归方程

已知某二元线性回归模型计算得到：

$$
\hat{\boldsymbol{\beta}}=
\begin{pmatrix}
-62.96\\
2.14\\
0.40
\end{pmatrix}
$$

其中第一项为截距，后两项分别为 $x_1,x_2$ 的系数。写出回归方程。

解：

根据多元线性回归形式：

$$
\hat{y}=\hat{\beta}_0+\hat{\beta}_1x_1+\hat{\beta}_2x_2
$$

代入系数：

$$
\hat{y}=-62.96+2.14x_1+0.40x_2
$$

结论：

- $x_1$ 每增加 1 个单位，在 $x_2$ 不变的条件下，$\hat{y}$ 平均增加 $2.14$。
- $x_2$ 每增加 1 个单位，在 $x_1$ 不变的条件下，$\hat{y}$ 平均增加 $0.40$。

<a id="sec-613"></a>

### 6.1.3 Logistic 回归

#### 1. 模型公式

Logistic 回归常用于因变量只有 0 和 1 两类结果的问题。

对数几率模型：

$$
\ln\left(\frac{p}{1-p}\right)
=
\beta_0+\beta_1x_1+\cdots+\beta_px_p
$$

概率形式：

$$
p=
\frac{e^{\beta_0+\beta_1x_1+\cdots+\beta_px_p}}
{1+e^{\beta_0+\beta_1x_1+\cdots+\beta_px_p}}
$$

也可写成：

$$
p=\frac{1}{1+e^{-z}},\quad z=\beta_0+\beta_1x_1+\cdots+\beta_px_p
$$

#### 【解题算法】Logistic 回归预测

1. 根据题目给出的系数写出线性部分 $z$。
2. 将样本的自变量取值代入，算出 $z$。
3. 用公式 $p=1/(1+e^{-z})$ 计算发生概率。
4. 与分类阈值比较，常用阈值为 0.5。
5. 若 $p\ge 0.5$，判为 1 类；若 $p<0.5$，判为 0 类。

#### 【例题 6-4】Logistic 回归分类

设某事件发生的 Logistic 回归模型为：

$$
z=-3+0.8x_1+0.5x_2
$$

当 $x_1=2,x_2=4$ 时，判断事件是否发生。分类阈值取 0.5。

解：

第一步，计算线性部分：

$$
z=-3+0.8\times 2+0.5\times 4
$$

$$
z=-3+1.6+2=0.6
$$

第二步，计算概率：

$$
p=\frac{1}{1+e^{-0.6}}\approx 0.646
$$

第三步，比较阈值：

$$
0.646>0.5
$$

结论：该样本判为 1 类，即事件预测为发生。

---

<a id="sec-62"></a>

## 6.2 判别分析

判别分析用于根据已知类别样本建立判别规则，再判断新样本属于哪一类。

<a id="sec-621"></a>

### 6.2.1 距离判别

#### 1. 两总体协方差矩阵相等时的判别函数

线性判别函数：

$$
W(\boldsymbol{x})=\boldsymbol{a}^{\text{T}}
(\boldsymbol{x}-\bar{\boldsymbol{\mu}})
$$

其中：

$$
\boldsymbol{a}
=
\boldsymbol{\Sigma}^{-1}
(\boldsymbol{\mu}_1-\boldsymbol{\mu}_2)
$$

$$
\bar{\boldsymbol{\mu}}
=
\frac{1}{2}
(\boldsymbol{\mu}_1+\boldsymbol{\mu}_2)
$$

判别规则：

- 若 $W(\boldsymbol{x})\ge 0$，则判定 $\boldsymbol{x}\in G_1$。
- 若 $W(\boldsymbol{x})<0$，则判定 $\boldsymbol{x}\in G_2$。

#### 【解题算法】距离判别

1. 写出两类总体均值向量 $\boldsymbol{\mu}_1,\boldsymbol{\mu}_2$。
2. 写出公共协方差矩阵 $\boldsymbol{\Sigma}$。
3. 求 $\boldsymbol{\Sigma}^{-1}$。
4. 计算 $\boldsymbol{a}=\boldsymbol{\Sigma}^{-1}(\boldsymbol{\mu}_1-\boldsymbol{\mu}_2)$。
5. 计算均值中点 $\bar{\boldsymbol{\mu}}=(\boldsymbol{\mu}_1+\boldsymbol{\mu}_2)/2$。
6. 将新样本 $\boldsymbol{x}$ 代入 $W(\boldsymbol{x})$。
7. 根据 $W(\boldsymbol{x})$ 的正负判断类别。

#### 【例题 6-5】距离判别分类

设：

$$
\boldsymbol{\mu}_1=
\begin{pmatrix}
2\\
3
\end{pmatrix},
\quad
\boldsymbol{\mu}_2=
\begin{pmatrix}
5\\
7
\end{pmatrix},
\quad
\boldsymbol{\Sigma}=
\begin{pmatrix}
1&0\\
0&1
\end{pmatrix}
$$

新样本：

$$
\boldsymbol{x}=
\begin{pmatrix}
3\\
4
\end{pmatrix}
$$

判断其类别。

解：

第一步，求判别系数：

$$
\boldsymbol{a}
=
\boldsymbol{\Sigma}^{-1}
(\boldsymbol{\mu}_1-\boldsymbol{\mu}_2)
=
\begin{pmatrix}
1&0\\
0&1
\end{pmatrix}
\begin{pmatrix}
-3\\
-4
\end{pmatrix}
=
\begin{pmatrix}
-3\\
-4
\end{pmatrix}
$$

第二步，求均值中点：

$$
\bar{\boldsymbol{\mu}}
=
\frac{1}{2}
\begin{pmatrix}
7\\
10
\end{pmatrix}
=
\begin{pmatrix}
3.5\\
5
\end{pmatrix}
$$

第三步，计算：

$$
\boldsymbol{x}-\bar{\boldsymbol{\mu}}
=
\begin{pmatrix}
3\\
4
\end{pmatrix}
-
\begin{pmatrix}
3.5\\
5
\end{pmatrix}
=
\begin{pmatrix}
-0.5\\
-1
\end{pmatrix}
$$

$$
W(\boldsymbol{x})
=
(-3,-4)
\begin{pmatrix}
-0.5\\
-1
\end{pmatrix}
=1.5+4=5.5
$$

结论：因为 $W(\boldsymbol{x})=5.5>0$，所以判定 $\boldsymbol{x}\in G_1$。

<a id="sec-622"></a>

### 6.2.2 贝叶斯判别

#### 1. 后验概率公式

贝叶斯判别根据后验概率最大原则分类：

$$
P(G_i|\boldsymbol{x})
=
\frac{p_if_i(\boldsymbol{x})}
{\sum_{j=1}^{k}p_jf_j(\boldsymbol{x})}
$$

其中：

- $p_i$：第 $i$ 类的先验概率。
- $f_i(\boldsymbol{x})$：在第 $i$ 类下观察到样本 $\boldsymbol{x}$ 的条件密度。

若各类服从正态分布且协方差矩阵相等，可用等价线性判别函数：

$$
g_i(\boldsymbol{x})
=
I_i^{\text{T}}\boldsymbol{x}+c_i+\ln p_i
$$

其中：

$$
I_i=\boldsymbol{\Sigma}^{-1}\boldsymbol{\mu}_i
$$

$$
c_i=-\frac{1}{2}
\boldsymbol{\mu}_i^{\text{T}}
\boldsymbol{\Sigma}^{-1}
\boldsymbol{\mu}_i
$$

判别规则：

$$
g_i(\boldsymbol{x})=\max_{1\le j\le k}g_j(\boldsymbol{x})
$$

则将 $\boldsymbol{x}$ 判入第 $i$ 类。

#### 【解题算法】贝叶斯判别

1. 写出各类别的先验概率 $p_i$。
2. 写出各类别均值 $\boldsymbol{\mu}_i$ 和公共协方差矩阵 $\boldsymbol{\Sigma}$。
3. 求 $\boldsymbol{\Sigma}^{-1}$。
4. 计算每一类的 $I_i=\boldsymbol{\Sigma}^{-1}\boldsymbol{\mu}_i$。
5. 计算每一类的 $c_i=-\frac12\boldsymbol{\mu}_i^{\text{T}}\boldsymbol{\Sigma}^{-1}\boldsymbol{\mu}_i$。
6. 对新样本分别计算 $g_i(\boldsymbol{x})$。
7. 比较所有 $g_i(\boldsymbol{x})$，数值最大的类别就是判别结果。

#### 【例题 6-6】贝叶斯线性判别

设有两类：

$$
\boldsymbol{\mu}_1=
\begin{pmatrix}
1\\
1
\end{pmatrix},
\quad
\boldsymbol{\mu}_2=
\begin{pmatrix}
4\\
5
\end{pmatrix},
\quad
\boldsymbol{\Sigma}=
\begin{pmatrix}
1&0\\
0&1
\end{pmatrix}
$$

先验概率：

$$
p_1=p_2=0.5
$$

新样本：

$$
\boldsymbol{x}=
\begin{pmatrix}
2\\
2
\end{pmatrix}
$$

解：

由于 $\boldsymbol{\Sigma}$ 是单位矩阵，所以 $\boldsymbol{\Sigma}^{-1}$ 仍为单位矩阵。

第一类：

$$
I_1=
\begin{pmatrix}
1\\
1
\end{pmatrix}
$$

$$
c_1=-\frac12(1^2+1^2)=-1
$$

$$
g_1(\boldsymbol{x})=(1,1)
\begin{pmatrix}
2\\
2
\end{pmatrix}
-1+\ln 0.5
$$

$$
g_1(\boldsymbol{x})=4-1-0.693=2.307
$$

第二类：

$$
I_2=
\begin{pmatrix}
4\\
5
\end{pmatrix}
$$

$$
c_2=-\frac12(4^2+5^2)=-20.5
$$

$$
g_2(\boldsymbol{x})=(4,5)
\begin{pmatrix}
2\\
2
\end{pmatrix}
-20.5+\ln 0.5
$$

$$
g_2(\boldsymbol{x})=18-20.5-0.693=-3.193
$$

结论：

$$
g_1(\boldsymbol{x})>g_2(\boldsymbol{x})
$$

所以 $\boldsymbol{x}$ 判入 $G_1$。

<a id="sec-623"></a>

### 6.2.3 Fisher 判别

Fisher 判别的核心思想是寻找一个投影方向，使得投影后“类间差异尽可能大，类内差异尽可能小”。

投影形式：

$$
y_r=\boldsymbol{a}_r^{\text{T}}\boldsymbol{x}
$$

#### 【解题算法】Fisher 判别思路

1. 计算各类样本均值。
2. 计算类内离差矩阵。
3. 计算类间离差矩阵。
4. 求使类间差异与类内差异比值最大的方向 $\boldsymbol{a}$。
5. 将样本投影到该方向上，得到一维判别值。
6. 根据判别值与临界点的距离判断类别。

#### 【例题 6-7】Fisher 判别的直观判断

若两类样本投影到某方向后，第 1 类均值为 3，第 2 类均值为 8，新样本投影值为 4。

解：

临界点可取两类均值中点：

$$
\frac{3+8}{2}=5.5
$$

因为：

$$
4<5.5
$$

所以新样本更接近第 1 类。

结论：新样本判入第 1 类。

---

<a id="sec-63"></a>

## 6.3 聚类分析

聚类分析是无监督学习方法，用于把相似样本归为同一类。

<a id="sec-631"></a>

### 6.3.1 距离与相似系数

#### 1. 常用距离公式

绝对值距离，也称曼哈顿距离：

$$
d(\boldsymbol{x},\boldsymbol{y})
=
\sum_{i=1}^{p}|x_i-y_i|
$$

欧氏距离：

$$
d(\boldsymbol{x},\boldsymbol{y})
=
\sqrt{\sum_{i=1}^{p}(x_i-y_i)^2}
$$

夹角余弦相似系数：

$$
c_{ij}
=
\frac{\sum_{k=1}^{n}x_{ki}x_{kj}}
{\sqrt{\sum_{k=1}^{n}x_{ki}^{2}}\sqrt{\sum_{k=1}^{n}x_{kj}^{2}}}
$$

#### 【解题算法】距离计算

1. 明确两个样本点的坐标。
2. 根据题目要求选择距离公式。
3. 逐维相减。
4. 曼哈顿距离取绝对值后求和。
5. 欧氏距离先平方、求和，再开方。
6. 若是相似系数，则计算内积并除以两个向量长度之积。

#### 【例题 6-8】计算两点距离

设：

$$
\boldsymbol{x}=(1,2,3),\quad \boldsymbol{y}=(4,2,1)
$$

求曼哈顿距离和欧氏距离。

解：

曼哈顿距离：

$$
d_1=|1-4|+|2-2|+|3-1|
$$

$$
d_1=3+0+2=5
$$

欧氏距离：

$$
d_2=\sqrt{(1-4)^2+(2-2)^2+(3-1)^2}
$$

$$
d_2=\sqrt{9+0+4}=\sqrt{13}
$$

结论：

- 曼哈顿距离为 $5$。
- 欧氏距离为 $\sqrt{13}$，约为 $3.606$。

<a id="sec-632"></a>

### 6.3.2 系统聚类法

系统聚类的基本思想：先把每个样本看成一类，再逐步合并距离最近的类，直到满足停止条件。

#### 1. 常见类间距离

最短距离法：

$$
D_{KL}=
\min_{i\in G_K,j\in G_L}d_{ij}
$$

合并 $G_K,G_L$ 为新类 $G_M$ 后：

$$
D_{MJ}=\min\{D_{KJ},D_{LJ}\}
$$

最长距离法：

$$
D_{KL}=
\max_{i\in G_K,j\in G_L}d_{ij}
$$

合并后：

$$
D_{MJ}=\max\{D_{KJ},D_{LJ}\}
$$

类平均法：

$$
D_{MJ}
=
\frac{n_K}{n_M}D_{KJ}
+
\frac{n_L}{n_M}D_{LJ}
$$

其中 $n_M=n_K+n_L$。

#### 【解题算法】系统聚类手工计算

1. 建立初始距离矩阵 $D_{(0)}$，每个样本单独成类。
2. 在非对角线元素中找最小距离。
3. 合并距离最小的两个类。
4. 根据所选方法更新新类与其他类之间的距离。
5. 得到新的距离矩阵。
6. 重复“找最小值、合并、更新矩阵”的过程。
7. 根据题目要求得到指定类数，或一直合并到一类。

#### 【例题 6-9】最短距离法系统聚类

设有三个样本 $A,B,C$，距离矩阵为：

| 样本 | A | B | C |
| --- | ---: | ---: | ---: |
| A | 0 | 2 | 6 |
| B | 2 | 0 | 4 |
| C | 6 | 4 | 0 |

用最短距离法进行第一次合并。

解：

第一步，找非对角线最小距离：

$$
d(A,B)=2
$$

这是最小值，所以先合并 $A$ 与 $B$，记为新类 $G_{AB}$。

第二步，更新新类与 $C$ 的距离。最短距离法取最小值：

$$
D_{AB,C}=\min\{D_{A,C},D_{B,C}\}
$$

$$
D_{AB,C}=\min\{6,4\}=4
$$

第一次合并后的距离矩阵：

| 类 | $G_{AB}$ | C |
| --- | ---: | ---: |
| $G_{AB}$ | 0 | 4 |
| C | 4 | 0 |

结论：第一次合并的是 $A$ 与 $B$，新类 $G_{AB}$ 与 $C$ 的距离为 $4$。

<a id="sec-633"></a>

### 6.3.3 动态聚类法 K-Means

K-Means 的目标是把样本分成 $k$ 类，使类内样本尽可能接近各自的中心。

#### 【解题算法】K-Means

1. 选定类别数 $k$。
2. 选取 $k$ 个初始聚类中心。
3. 计算每个样本到各聚类中心的距离。
4. 将每个样本分到最近的中心所在类别。
5. 对每一类重新计算均值，作为新的聚类中心。
6. 比较新旧中心是否变化。
7. 若中心变化，则返回第 3 步继续迭代。
8. 若中心不变，聚类结束。

#### 【例题 6-10】K-Means 一轮迭代

设一维样本为：

$$
1,\ 2,\ 8,\ 9
$$

取 $k=2$，初始中心为：

$$
C_1=1,\quad C_2=9
$$

进行一轮 K-Means 聚类。

解：

第一步，计算距离并分配类别：

| 样本 | 到 $C_1=1$ 的距离 | 到 $C_2=9$ 的距离 | 所属类别 |
| --- | ---: | ---: | --- |
| 1 | 0 | 8 | 第 1 类 |
| 2 | 1 | 7 | 第 1 类 |
| 8 | 7 | 1 | 第 2 类 |
| 9 | 8 | 0 | 第 2 类 |

第二步，更新中心：

第 1 类样本为 $1,2$：

$$
C_1'=\frac{1+2}{2}=1.5
$$

第 2 类样本为 $8,9$：

$$
C_2'=\frac{8+9}{2}=8.5
$$

结论：一轮迭代后，新中心为 $C_1'=1.5,C_2'=8.5$。若下一轮类别分配不再改变，则算法收敛。

---

<a id="sec-64"></a>

## 6.4 主成分分析

主成分分析用于把多个相关变量转化为少数几个互不相关的综合变量，并尽量保留原始信息。

#### 1. 主成分公式

设协方差矩阵或相关矩阵的特征值为：

$$
\lambda_1\ge \lambda_2\ge \cdots \ge \lambda_p\ge 0
$$

对应单位特征向量为：

$$
\boldsymbol{t}_1,\boldsymbol{t}_2,\cdots,\boldsymbol{t}_p
$$

第 $i$ 个主成分为：

$$
y_i=\boldsymbol{t}_i^{\text{T}}\boldsymbol{x}
$$

第 $i$ 个主成分的贡献率：

$$
\frac{\lambda_i}{\sum_{j=1}^{p}\lambda_j}
$$

前 $m$ 个主成分的累计贡献率：

$$
\frac{\sum_{i=1}^{m}\lambda_i}{\sum_{j=1}^{p}\lambda_j}
$$

#### 【解题算法】主成分分析

1. 对原始数据进行标准化，尤其是变量量纲不同的时候。
2. 计算相关矩阵 $\boldsymbol{R}$ 或协方差矩阵 $\boldsymbol{S}$。
3. 求矩阵的特征值 $\lambda_i$。
4. 将特征值从大到小排序。
5. 求对应的单位特征向量 $\boldsymbol{t}_i$。
6. 计算各主成分贡献率。
7. 计算累计贡献率，通常保留累计贡献率达到 80% 到 90% 的前几个主成分。
8. 写出保留主成分的线性表达式。
9. 若需要综合评价，可按贡献率加权计算综合得分。

#### 【例题 6-11】根据特征值选择主成分

设某相关矩阵的特征值为：

$$
\lambda_1=2.4,\quad \lambda_2=1.2,\quad \lambda_3=0.4
$$

问至少保留几个主成分才能使累计贡献率达到 80%。

解：

总特征值：

$$
\lambda_1+\lambda_2+\lambda_3=2.4+1.2+0.4=4.0
$$

第 1 个主成分贡献率：

$$
\frac{2.4}{4.0}=0.6=60\%
$$

前 2 个主成分累计贡献率：

$$
\frac{2.4+1.2}{4.0}=0.9=90\%
$$

结论：保留前 2 个主成分即可，因为累计贡献率 $90\%>80\%$。

---

<a id="sec-65"></a>

## 6.5 因子分析

因子分析用于用少数公共因子解释多个观测变量之间的相关关系。

<a id="sec-651"></a>

### 6.5.1 因子载荷矩阵的主成分法估计

#### 1. 因子载荷矩阵

利用前 $m$ 个特征值和对应特征向量估计因子载荷矩阵：

$$
\hat{\boldsymbol{A}}
=
(
\sqrt{\hat{\lambda}_1}\hat{\boldsymbol{t}}_1,
\sqrt{\hat{\lambda}_2}\hat{\boldsymbol{t}}_2,
\cdots,
\sqrt{\hat{\lambda}_m}\hat{\boldsymbol{t}}_m
)
$$

公共性方差：

$$
h_i^2=\sum_{j=1}^{m}a_{ij}^{2}
$$

特殊方差：

$$
\sigma_i^2=1-h_i^2
$$

#### 【解题算法】主成分法估计因子载荷

1. 求相关矩阵 $\boldsymbol{R}$ 的特征值和特征向量。
2. 选择前 $m$ 个公共因子。
3. 对每个保留的特征值开平方。
4. 用 $\sqrt{\lambda_j}$ 乘以对应特征向量 $\boldsymbol{t}_j$。
5. 把得到的列向量组成因子载荷矩阵 $\hat{\boldsymbol{A}}$。
6. 对每个变量，把该行载荷平方后求和，得到公共性方差 $h_i^2$。
7. 用 $1-h_i^2$ 得到特殊方差。

#### 【例题 6-12】计算公共性方差和特殊方差

设某变量在两个公共因子上的载荷为：

$$
a_{i1}=0.8,\quad a_{i2}=0.3
$$

求该变量的公共性方差和特殊方差。

解：

公共性方差：

$$
h_i^2=a_{i1}^2+a_{i2}^2
=0.8^2+0.3^2
$$

$$
h_i^2=0.64+0.09=0.73
$$

特殊方差：

$$
\sigma_i^2=1-h_i^2=1-0.73=0.27
$$

结论：

- 公共因子解释了该变量方差的 $73\%$。
- 仍有 $27\%$ 属于特殊方差，不能被公共因子解释。

<a id="sec-652"></a>

### 6.5.2 主因子法

主因子法与主成分法的区别在于：主因子法不直接使用相关矩阵主对角线上的 1，而是用公共性方差的初始估计值替代。

约相关矩阵：

$$
\boldsymbol{R}^{*}
$$

其主对角线元素通常为公共性方差的初始估计 $\hat{h}_i^2$。

#### 【解题算法】主因子法

1. 计算原始相关矩阵 $\boldsymbol{R}$。
2. 估计每个变量的初始公共性方差 $\hat{h}_i^2$。
3. 用 $\hat{h}_i^2$ 替换 $\boldsymbol{R}$ 主对角线上的 1，得到约相关矩阵 $\boldsymbol{R}^{*}$。
4. 对 $\boldsymbol{R}^{*}$ 求特征值和特征向量。
5. 选取公共因子个数 $m$。
6. 计算因子载荷矩阵。
7. 如需更精确，可根据新的公共性方差继续迭代。

#### 【例题 6-13】构造约相关矩阵

设三个变量的相关矩阵为：

$$
\boldsymbol{R}=
\begin{pmatrix}
1&0.6&0.4\\
0.6&1&0.5\\
0.4&0.5&1
\end{pmatrix}
$$

初始公共性方差估计为：

$$
\hat{h}_1^2=0.7,\quad \hat{h}_2^2=0.8,\quad \hat{h}_3^2=0.6
$$

构造约相关矩阵。

解：

将主对角线上的 1 分别替换为 $0.7,0.8,0.6$，非对角线保持不变：

$$
\boldsymbol{R}^{*}=
\begin{pmatrix}
0.7&0.6&0.4\\
0.6&0.8&0.5\\
0.4&0.5&0.6
\end{pmatrix}
$$

结论：该矩阵就是主因子法后续求特征值和特征向量时使用的约相关矩阵。

<a id="sec-653"></a>

### 6.5.3 因子旋转与因子得分

#### 1. 因子旋转

当因子载荷矩阵不便解释时，可进行因子旋转：

$$
\boldsymbol{A}^{*}=\boldsymbol{A}\boldsymbol{T}
$$

其中 $\boldsymbol{T}$ 是正交矩阵。旋转不会改变模型整体解释能力，但能让因子含义更清晰。

#### 2. 因子得分

回归法估计因子得分：

$$
\hat{\boldsymbol{f}}
=
\boldsymbol{A}^{\text{T}}
\boldsymbol{R}^{-1}
\boldsymbol{X}
$$

#### 【解题算法】因子得分计算

1. 得到因子载荷矩阵 $\boldsymbol{A}$。
2. 得到相关矩阵 $\boldsymbol{R}$ 并求逆。
3. 对样本数据进行标准化，得到 $\boldsymbol{X}$。
4. 代入 $\hat{\boldsymbol{f}}=\boldsymbol{A}^{\text{T}}\boldsymbol{R}^{-1}\boldsymbol{X}$。
5. 若有多个因子，可得到每个样本在各公共因子上的得分。
6. 如需综合排名，可按照因子贡献率对因子得分加权。

#### 【例题 6-14】因子旋转的解释

某因子载荷矩阵旋转前为：

| 变量 | 因子 1 | 因子 2 |
| --- | ---: | ---: |
| $X_1$ | 0.65 | 0.61 |
| $X_2$ | 0.70 | 0.58 |
| $X_3$ | 0.20 | 0.82 |

旋转后为：

| 变量 | 因子 1 | 因子 2 |
| --- | ---: | ---: |
| $X_1$ | 0.88 | 0.15 |
| $X_2$ | 0.91 | 0.10 |
| $X_3$ | 0.12 | 0.84 |

解释旋转的作用。

解：

旋转前，$X_1,X_2$ 在两个因子上载荷都较高，不容易判断它们主要属于哪个因子。

旋转后：

- $X_1,X_2$ 在因子 1 上载荷很高，在因子 2 上载荷很低。
- $X_3$ 在因子 2 上载荷很高，在因子 1 上载荷很低。

结论：旋转使载荷结构更清晰，便于解释因子含义。

---

<a id="sec-66"></a>

## 6.6 典型相关分析

典型相关分析用于研究两组变量之间的整体相关关系。

#### 1. 基本思想

设第一组变量为：

$$
\boldsymbol{X}=(X_1,X_2,\cdots,X_p)^{\text{T}}
$$

第二组变量为：

$$
\boldsymbol{Y}=(Y_1,Y_2,\cdots,Y_q)^{\text{T}}
$$

典型相关分析要寻找两组线性组合：

$$
U=a_1X_1+a_2X_2+\cdots+a_pX_p
$$

$$
V=b_1Y_1+b_2Y_2+\cdots+b_qY_q
$$

使 $U$ 和 $V$ 的相关系数达到最大。

若通过特征方程求得特征值 $\lambda_k$，则第 $k$ 对典型变量的典型相关系数为：

$$
\rho_k=\sqrt{\lambda_k}
$$

#### 【解题算法】典型相关分析

1. 将变量分为两组 $\boldsymbol{X}$ 和 $\boldsymbol{Y}$。
2. 计算协方差矩阵或相关矩阵。
3. 将矩阵按变量组分块，得到 $\boldsymbol{\Sigma}_{11},\boldsymbol{\Sigma}_{12},\boldsymbol{\Sigma}_{21},\boldsymbol{\Sigma}_{22}$。
4. 构造并求解典型相关分析对应的特征方程。
5. 得到特征值 $\lambda_k$。
6. 计算典型相关系数 $\rho_k=\sqrt{\lambda_k}$。
7. 根据特征向量写出典型变量 $U_k,V_k$。
8. 根据 $\rho_k$ 的大小解释两组变量的相关强度。

#### 【例题 6-15】由特征值求典型相关系数

设典型相关分析得到两个特征值：

$$
\lambda_1=0.81,\quad \lambda_2=0.36
$$

求典型相关系数。

解：

第 1 对典型变量的相关系数：

$$
\rho_1=\sqrt{0.81}=0.9
$$

第 2 对典型变量的相关系数：

$$
\rho_2=\sqrt{0.36}=0.6
$$

结论：

- 第一对典型变量相关性较强，相关系数为 $0.9$。
- 第二对典型变量相关性中等，相关系数为 $0.6$。

---

## 本章解题总口诀

| 方法 | 看到题目时先想什么 | 最关键计算 | 最后怎么判断 |
| --- | --- | --- | --- |
| 一元回归 | 一个 $x$ 解释一个 $y$ | $L_{xx},L_{xy},\hat{\beta}_0,\hat{\beta}_1$ | 写方程、预测、做显著性检验 |
| 多元回归 | 多个 $x$ 解释一个 $y$ | $(X^TX)^{-1}X^TY$ | 写多元方程，解释各系数 |
| Logistic 回归 | 结果是 0 或 1 | $p=1/(1+e^{-z})$ | 与 0.5 或指定阈值比较 |
| 距离判别 | 已知类别均值，判断新样本 | $W(x)$ | 看 $W(x)$ 正负 |
| 贝叶斯判别 | 有先验概率和密度 | $g_i(x)$ 或后验概率 | 哪个最大归哪类 |
| 系统聚类 | 逐步合并样本 | 距离矩阵更新 | 最小距离先合并 |
| K-Means | 给定 $k$ 个类别 | 分配样本、更新中心 | 中心不变则停止 |
| 主成分分析 | 降维保留信息 | 特征值、贡献率 | 累计贡献率达到要求 |
| 因子分析 | 少数公共因子解释变量 | 因子载荷、公共性方差 | 看载荷大小解释因子 |
| 典型相关分析 | 两组变量整体相关 | $\rho_k=\sqrt{\lambda_k}$ | 相关系数越大关系越强 |
