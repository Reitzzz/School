# 第 01 模块作业整理笔记

### 1.1 单选题

#### 题干

1. 数据库 DB、数据库应用程序 DB application 和数据库系统 DBS 之间的关系是什么？

   - A. 一个数据库系统包含多个数据库应用程序，这些应用程序访问数据库。
   - B. 一个数据库应用程序只包含一个数据库系统，该系统访问数据库。
   - C. 一个数据库包含多个数据库应用程序，这些应用程序访问数据库系统。
   - D. 一个数据库应用程序包含多个数据库，这些数据库访问数据库系统。

2. 在数据库系统中，数据独立性指什么？

   - A. 应用程序不受数据值变化的影响。
   - B. 应用程序不受数据量变化的影响。
   - C. 应用程序不受数据结构变化的影响。
   - D. 数据存储结构不受数据存储方式变化的影响。

3. 数据库系统中保证数据独立性的是哪一项？

   - A. 模式和外模式。
   - B. 模式和内模式。
   - C. 三级模式之间的两级映像。
   - D. 三级模式。

4. 用户和应用程序都能看到的数据库逻辑结构描述称为什么？

   - A. 概念模式。
   - B. 内模式。
   - C. 外模式。
   - D. 物理模式。

5. 下列哪项不是 DBA 的职责？

   - A. 定义完整性约束。
   - B. 定义数据库模式。
   - C. 管理数据库安全。
   - D. 设计数据库管理系统。

6. 下列哪项操作不是 DDL 支持的操作？

   - A. 在数据库中创建视图。
   - B. 修改数据库中的表结构。
   - C. 从表中查找若干行。
   - D. 在数据库中创建表。

7. 数据模型由哪三个组成部分构成？

   - A. 外模式、概念模式和内模式。
   - B. 关系、元组和主键。
   - C. 数据结构、数据操作和完整性规则。
   - D. 模型含义、操作规则和实现语言。

8. 关系数据模型由哪些内容组成？

   - A. 关系数据结构、关系操作和完整性约束。
   - B. 关系、元组和主键。
   - C. 实体完整性、参照完整性和用户自定义完整性。
   - D. 外模式、概念模式和内模式。

9. 数据库系统之所以具有数据独立性，是因为什么？

   - A. 基于磁盘的存储。
   - B. 三级模式结构。
   - C. 通过调用操作系统访问数据。
   - D. 可视化程序设计语言。

10. 在数据库系统中，“数据独立性”是什么意思？

    - A. 一个数据独立于另一个数据。
    - B. 应用程序独立于数据库结构。
    - C. 磁盘独立于内存。
    - D. 数据独立于磁盘。

#### 答案

```text
1. A
2. C
3. C
4. A
5. D
6. C
7. C
8. A
9. B
10. B
```

#### 解析

- DB system 可以包含多个数据库应用程序，应用程序访问数据库。
- 数据独立性强调应用程序不因数据库结构变化而频繁修改。
- 三级模式两级映像是实现数据独立性的关键。
- 概念模式描述数据库的整体逻辑结构。
- DBA 负责数据库管理，不负责设计 DBMS 软件本身。
- DDL 用于定义表、视图和结构，查询行属于 DML/DQL。
- 数据模型通常包括数据结构、数据操作和完整性约束。
- 关系数据模型由关系数据结构、关系操作、完整性约束组成。

### 1.2 简答题：什么是数据模型？

#### 题干

什么是数据模型？讨论数据模型的主要类型。

#### 答案

数据模型是用于描述数据、数据之间的联系、数据语义以及一致性约束的一组概念集合。

主要类型：

```text
概念数据模型
逻辑数据模型
物理数据模型
```

#### 解析

概念数据模型面向现实世界建模，例如 E-R 模型；逻辑数据模型面向 DBMS 的组织方式，例如关系模型；物理数据模型关注数据在存储介质上的组织和访问方式。

### 1.3 名词解释

#### 题干

解释以下术语：data、Database、database management system、data independence、Security、Integrity、Views、data administrator、physical database designer、application developer、end-users、database administrator、logical database designer。

#### 答案

| 名词 | 解释 |
| --- | --- |
| data | 描述事物的符号记录，是数据库中存储的基本对象。 |
| Database | 长期存储在计算机内、有组织、可共享的大量数据集合。 |
| DBMS | 位于用户与操作系统之间的数据管理软件，用于定义、组织、存储、管理和维护数据。 |
| data independence | 应用程序与数据库数据结构之间相互独立，包括逻辑数据独立性和物理数据独立性。 |
| Security | 防止未授权访问、修改或破坏数据库。 |
| Integrity | 保证数据正确、有效、相容。 |
| Views | 从一个或多个基本表导出的虚拟表，只保存定义，不一定实际存储数据。 |
| data administrator | 管理组织数据资源，制定数据标准、政策并参与概念/逻辑设计的人员。 |
| physical database designer | 决定逻辑设计如何在存储设备上物理实现的人员。 |
| application developer | 编写数据库应用程序，为用户提供业务功能的人员。 |
| end-users | 通过应用程序或查询语言使用数据库的最终用户。 |
| DBA | 负责数据库物理设计、日常管理、安全、备份恢复、完整性和性能监控的人员。 |
| logical database designer | 识别实体、属性、联系和约束，完成数据库逻辑结构设计的人员。 |

#### 解析

这些概念可以按“数据对象、管理软件、使用人员、控制目标”来记忆：Database 是数据集合，DBMS 是管理软件，DBA/设计者/开发者/用户是角色，Security 和 Integrity 是数据库管理目标。

---




